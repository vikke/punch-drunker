<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mail %>
    <%= f.text_field :mail %>
  </div>

  <div class="field">
    <%= f.label :provider %>
    <%= f.text_field :provider %>
  </div>

  <div class="field">
    <%= f.label :oauth_token %>
    <%= f.text_field :oauth_token %>
  </div>

  <div class="field">
    <%= f.label :punch_token %>
    <%= f.text_field :punch_token %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
